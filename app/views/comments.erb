<div class="container">

<h2><a href="<%= @post.url %>"><%= @post.title %></a></h2>
<h3><%= @comments.count %> Comments</h3>

<% @comments.each do |comment| %>
  <div class="author"><%= comment.user.username %></div>
  <div class="unthreaded-comments"> <%= comment.content %></div>
<%end %>

</div>
